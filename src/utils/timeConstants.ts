/**
 * 時間計算用の定数
 */

/** ミリ秒から時間への変換定数 (1000ms * 60s * 60min) */
export const MS_TO_HOURS = 1000 * 60 * 60;

/** ミリ秒から分への変換定数 (1000ms * 60s) */
export const MS_TO_MINUTES = 1000 * 60;

/** ミリ秒から秒への変換定数 */
export const MS_TO_SECONDS = 1000;
